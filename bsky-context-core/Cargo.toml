[package]
name = "bsky-context-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Crawl and render Bluesky conversation graphs (replies + quote posts)"

[dependencies]
az = "1.3.0"
futures = "0.3"
indexmap = { version = "2", features = ["serde"] }
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"

[lints]
workspace = true
